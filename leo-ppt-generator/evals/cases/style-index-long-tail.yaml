id: style-index-long-tail
title: 新来源模板在名称索引可达
description: 验证统一风格索引方案的实际工具与回答边界
context:
  files:
    review-case.json: '{"case_id":"style-index-long-tail"}'
input:
  prompt: |
    使用 $leo-ppt-generator，advise 模式，只咨询。请核对地图战略风和流光液态风是否在库；只读必要生成 Markdown，说明可达性与验证证据边界，不生成页面。
constraints:
  timeout_seconds: 600
  max_turns: 12
judge:
  type: script
  script_path: evals/judges/judge_style_index.py
  timeout_seconds: 15
