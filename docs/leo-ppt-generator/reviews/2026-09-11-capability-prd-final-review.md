# 能力提升 PRD 终审（会议纪要 · 第四轮 · 签发门）

- 日期：2026-09-11
- 评审对象：[PRD v1.5](../../prd/2026-09-11-leo-ppt-capability-improvement-prd.md)
- 形式：三位终审专家独立审查（发布门 / 对抗终审 / 实现方验收），其中实现方**仅凭 PRD** 开工、并抽验源码事实；三份报告 `expert-reports/final-a-release-gate.md`、`final-b-adversarial.md`、`final-c-implementer.md`
- 结论：**不可签发**（2 票否决性结论、1 票有条件签发）→ 修订为 v1.6 重提

## 1. 终审结论

| 专家 | 立场 | 结论 | blocker 数 |
| --- | --- | --- | --- |
| A 发布门 | 吸收闭合与自洽 | **有条件签发** | 2 |
| B 对抗终审 | 尽力否掉 | **否掉** | 3 |
| C 实现方验收 | 仅凭文档能否开工 | **需补文档后才能开工** | 3 |

三方独立命中的共同硬伤（最高置信）：

1. **R-77 最小子集无法判定完成** —— 三条验收全为【单测】（§2.5 自己禁止用 proxy 宣称行为证据），且验收 2 保留「缺失指标如实 `not_recorded`」这条**万能通过出口**：全套指标皆缺仍算过关。它是 R-70 否决门、R-71 效果验收、R-74 口径、R-73/R-84 数据的唯一输入——**全批的测量底座可以不测出任何东西而宣告完成**（B G2-2、C G1/G2/G3、A G3）。
2. **`page-type-regime-v1` 已并发落地，PRD 零登记且已被证伪一项状态** —— B 实证：`template-library/governance/rules/page-type-regime-v1.json`（mtime 12:25）、`runtime/src/leo_ppt_generator/page_intent.py`（12:26）、`tests/test_page_intent_routing.py`（12:34）、`docs/plans/2026-09-11-002-*.md`（12:34）均在未提交工作区，`CHANGELOG.md [Unreleased]` 已完整登记；PRD v1.5 写于 12:39 且零提及。C 独立核对确认 `page_intent.py` 已实装。**后果**：PRD 的 `page_type_regime`（页型枚举×lane）与 CLI `--page-type`（chart/text-heavy/image）同名异义，且 §8 中「R-74 状态＝待页型 regime」在写出时即为假（3a 合同已在消费 regime）。
3. **回放集与校准集（defect-controls）无归属、无验收、工程量漏项** —— R-70 验收 4、R-71 验收 4、盲评对照分层全部建在回放集上，而它只出现在 R-70 自己的验收句里，R-77 验收零断言、§8 工程量无行；校准集同理（§1.5 已承认「物理未落盘」，§8 R-70 工程量未列）。

## 2. 采纳的裁定（合计 44 findings，全部落 v1.6）

### 阻断级（不开工即无法推进）

| # | 裁定 | 来源 |
| --- | --- | --- |
| T1 | **R-77 交付物三定**：入口＝`scripts/run_quality_scorecard.py <run>`（stdout JSON＋落 `<run>/reports/quality-scorecard.json`）；注册表＝`references/metrics-registry.md`（含 schema 与字段表，附录 A10）；成本对账＝Σ backend_stats.tokens×单价 vs `estimate_run_cost` 区间，口径版本 `cost-caliber-v1` | C G3-1/G3-2/G3-3、A G8、B G2-2 |
| T2 | **删除 `not_recorded` 通过地位**：R-77 最小子集须在 ≥1 个真实历史 run 上产出**非 not_recorded** 的 TF 触发率与成本对账数值，并写出口径版本 id；全部候选 run 皆 not_recorded ⇒ 判 **blocked（非 pass）** 并触发数据饥饿决策记录，**R-70 否决门不得在 blocked 下放行** | B G2-2 |
| T3 | **并发落地登记**：§1.5 增补基线第 9 条（`page-type-regime-v1` 已落地事实、消费点、lint/回归缺口）；术语收敛——语义层称 `page_type_regime`（页型），CLI 层改称 `content_class`（`chart|text-heavy|image`＋新增 `render`）；A6 由「schema 草案」改为「**v1 审计与扩展清单**（补 composite lane、render 类、去 fixture 缺省、版本戳）」；R-74 状态由「待页型 regime」改为「**v1 已并发落地；待做＝扩展与门禁接入**」，工程量 M→M− | B G2-1/G2-12、C G3-6 |
| T4 | **治理两条新条款**：①**并发落地登记**（任何落地若触碰本 PRD 声明的工件，须在同一变更内更新对应条目的状态/证据锚/最后验证日期并登记修订记录；未登记即视为漂移，相关条目暂停推进）②**条目准入复核**（依赖工件须过三问：可被 lint/schema 校验？回归可被仓库运行器发现？口径可在本 PRD 内查？任一为否则该条目的门不得开工） | B 治理节 |
| T5 | **回放集与校准集定归属**：回放集（固定母版 md＋required_text＋TF 事件标注＋母版→PAGES 转换＋冻结页图）**归 R-77 随条交付**并登记内容 sha256，R-70 验收 4 与 R-71 验收 4 的基线数值必须引用该 sha256；校准集 `defect-controls`（健康基线＋单缺陷植入副本，平移 `verify_industry_visual_matrix.run_calibration`）**归 R-70 先决第 4 项**，R-70a 开票前交付；§8 R-70 工程量补列 | B G2-3 |

### 高优级

| # | 裁定 | 来源 |
| --- | --- | --- |
| T6 | **效力降级判定式可判化**：预登记须含口径版本 id／基线事件数／统计量／判定阈值／记录路径；降级判定式写成「点估计降幅 ≥80% **且** 率比 95% CI 下界 >1.0」；**CI 含 1.0 或下界 ≤1.0 ⇒ R-70c 度量门判 not_passed**，只能以窄口径（构造性错字消除＋成本中性）验收，交付披露须原文写出「未观测到降幅证据」；「全宽披露」不构成通过条件 | B G2-4 |
| T7 | **三阶段止损与产能**：R-70a/b/c 各配止损动作（a 前 spike 不过且备选不可行 ⇒ R-70 整条推入波 2、合同修订冻结；a 过 b 盲评不过 ⇒ a 为终态、冻结 b/c、资产登记沉没；R-71 性能 spike 不过 ⇒ 阈值调整须登记调整前后判据与依据，禁止直接调大 ≤60s）；波 1 同时活跃条目上限＝1；超过预计周数 1.5 倍未过门即按止损阶梯处置 | B G2-17、G2-5 |
| T8 | **沉没成本清单立为 §9.1**（开工前冻结）：3 类版式×2 主题资产、42 版式 media 槽改造、透明文字层输出、双 provenance schema、判官图片通道与评分 schema、校准集、回放集 image lane 付费冻结——每项标注可复用于其他条目/不可复用与放弃处置 | B G2-5 |
| T9 | **开工前冻结决策表**（§3）：D-1 `text_fallback` 补登路线（a′ 侧车 / 改文档口径，默认 a′；影响 TF 分子与效力反算基数）；D-2 偏离率基线（top2 / suggest_layout）；D-3 未选定绑定页缺省来源（推荐序首个 / 预编译缓存）——各注明冻结时点、决策记录路径（`<project>/content/decision-log.md`）、影响条目 | B G2-6、C G3-4/G3-5 |
| T10 | **盲评三个数值落定**：非劣判定式＝双判官均分 95% 区间下界 ≥ 对照均分 −0.5；融合度单项线＝均分 ≥3.0 且任一判官 ≤2 即否决；对照组＝文本密集 ≥2 组、氛围 ≥1 组，**每组 ≥N 页对**（N 由效力反算给出并预登记，未达则不得宣称非劣） | B G2-10、A G7 |
| T11 | **测试入口标签新增两档**：【判官-fixture】（自动判读，附 fixture 哈希与重跑命令）、【回放集-proxy】（聚合代理指标，不得声称行为级证据）；R-70-2 改【判官-fixture＋人工-协议】、R-70-5 与 R-71-4 改【回放集-proxy】、R-71-5 与 R-76-3 附协议路径 | B G2-11、A G12 |
| T12 | **母版 schema 属主＝R-71**（首个把母版当机器工件消费的条目）：登记母版字段 schema（含 R-70b 文字层声明、R-82 页级预算时长、R-75 提案落点、R-74 lane 声明）；其余条目的字段需求须在冻结前提交，之后走加性变更带版本号 | B G2-7 |
| T13 | **R-74 去饱和断言**：语义加分不得改变 clamp 前的高分候选序（实现须把语义项移出 clamp 或抬高上限），【单测】红例＝同页首选与回退候选 `score` 相等；ground truth 标注须声明所对照的 `suggest_layout` 版本与是否去饱和；R-77 增指标「语义并列（tie）比例」 | B G2-9 |
| T14 | **R-78 拼图路径定名** `<run>/diffs/`（与 `reports/render-preview/**`、`final/render-preview/**` 均不重叠，故不在 `render_previews` 指纹内）；R-71 增**路径三分收敛口径**（`reports|final/render-preview/**`＝成品预览，在指纹内；`<run>/previews/`＝骨架，不在；`<run>/diffs/`＝拼图，不在） | C G3-7、B G2-15 |
| T15 | **维护债5 修复方向定死**：runtime 观察哨行改 `page` 为字符串并补 `schema_version: 1`（不改 agent 侧写侧契约）；`record_run_step._page_sort_key` 增 `str()` 容错为第二道防线 | C G3-8、A G10 |
| T16 | **TF 分子来源定序**：优先取 `sources-manifest.json` 中 `source_class=deterministic-overlay` 的页；该 run 无 `--sources` 冻结时回退 `backend_stats` sidecar 的 `text_fallback`；两来源合并计数并在记分卡标注 `tf_source` | C G3-9、A G8 |
| T17 | **R-77 与 suggest_layout 关系裁决**：R-77 只把 `allocate_deck.top2` 改为纯 `_soft_rank` 序（`_selection_result` 增 `profiles` 或由 `allocate_deck` 直接产出 top2）；`scripts/suggest_layout.py` 保持独立加权排序**不合并**，两者偏离率作为观察指标 | C G3-10、B G2-9 |
| T18 | **R-81 净影响主张修正**：跨域（image/edit 页并行）等待削减经外部 Agent 用满 `max_concurrent_workers` 获得；**同域（image 域内）仍受每域单锁＋全局 revision CAS 限制、每波仅一人成功**——该限制由 R-77 调度纪律告警观察，不计入本批收益宣称 | B G2-8 |

### 常规级（随定稿一并修）

T19 计数口径统一（§1 的「活跃 13 条」→「在册 13：12 活跃＋R-83 暂缓」）；T20 波次枚举补 R-80 与「77 全量」、§8 R-77 与 R-80 波次列修正；T21 R-72 重启条件三行展开；T22 补共创模式限定句；T23 「合成产物纳入指纹链」写入 R-70b 范围；T24 §8 服务层列填实（R-78 人决策/R-79 确定性/R-80 确定性/R-82 确定性）；T25 §8 增列「最后验证日期/证据锚/检查者/口径版本/并发状态」；T26 附录 A1 协议化（四段实质内容）＋A1~A6 标注来源 finding；T27 新增 §2.4.1 术语与编号索引（TF-1/TF-2/3a/U2-U4/U8/WS6/M0/五类指纹/page_type_regime vs content_class/composite/回放集/defect-controls 各附锚点）；T28 悬空引用修正（终审纪要路径、§7 lightbox 指针、§8「见各条」）；T29 R-78/R-79 补负例；T30 漂移清单补第 9 项（page_intent/regime 基线遗漏）；T31 分母归一化单位「每百页」补回；T32 效力相关「每评审周期复核」改为「每 2 周复核一次，连续两次无变化触发库存裁剪决议」。

## 3. 部分采纳与说明

- **B 建议的重提形态（只做 R-77 最小子集＋R-71，R-70 整条推入波 2）**：作为**备选形态**采纳进 §3，但不代为决定——这是范围决策，须由需求方选（见 §5 待决项 1）。
- **B G2-4 对「未达规模出口」的批评**：采纳其对判定式的可判化要求（T6），但保留"未达规模如实报告"作为**披露义务**（与 R-73 同标准），不再作为通过条件。
- **C 提出的 `image allocate` 子命令命名**：采纳为建议入口（T9 D-3），实现在开工前冻结决策时确认。
- 驳回：无。

## 4. 修正后的重提要求（v1.6 齐备即放行，全部为文档级修改，无需新代码）

① T1~T5 四项阻断级；② T6~T13 高优级；③ T14~T18 高优级；④ T19~T32 常规级。以上齐备后由发布门复核（本轮三位之一）即可签发，不需新开评审轮。

## 5. 待决项（须需求方决定，不属评审范围）

1. **波 1 范围**：(a) 维持五条（R-77/R-71/R-70a-b-c/R-76 一期/R-84，配 T7 止损）；或 (b) 收窄为 R-77 最小子集＋R-71，R-70 整条推入波 2（B 推荐，可显著降低单人维护风险，代价是文字保真问题延后）。
2. **R-74 与已落地 `suggest_layout` 的关系**：T17 已给出「不合并、双轨对账」的裁决，若需求方希望统一为单一排序器，需另行决议（影响 R-74 范围与工程量）。

## 6. 会议说明

本轮终审的价值在于三点：①它证明了前三轮的"吸收闭合"并不等于"文档与现实一致"——PRD 自 v1.3 起就把 `page_type_regime` 当作待建项，而实现已于同日落地，三代评审（含专门读源码的第三轮）都未把它纳入基线；②它发现全批的测量底座可以"空转完成"，这是所有门禁的共同上游；③它把 8 项被文档推给"实现时决定"的悬置决策中的 3 项判定为必须开工前冻结。

v1.5 条目完整性（四要素恢复）经 C 确认成立；本轮的否决针对的是**基线事实、交付物定义与门链可满足性**，非文风或细节。
