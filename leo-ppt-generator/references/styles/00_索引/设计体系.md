# 设计体系（元结构）

> 借鉴 `ppt-master`（hugohe3/ppt-master）的四轴正交模型，重构 leo-ppt 风格库的认知结构。核心结论：**风格不是「一个完整配方」，而是「几个正交轴的组合」**。

## 一、核心原则

### 1. 轴正交，任意组合

一个 deck 的最终视觉，由**四个独立轴**分别锁定，再任意组合：

| 轴 | 回答 | 例子 |
|---|---|---|
| **视觉风格** visual-style | 长什么样（形状/留白/纹理/排版） | swiss-minimal、glassmorphism、editorial、ink-wash |
| **论证模式** mode | 怎么论证（叙事骨架/页面节奏） | pyramid、narrative、instructional、showcase、briefing |
| **结构布局** layout | 什么画布 + 什么版式骨架 | 16:9 / 4:3 / 9:16 / 3:4 / 1:1 |
| **品牌身份** brand | 谁的身份（企业 VI / 行业惯例） | mckinsey、bcg、google、huawei |

> 关键区分：**视觉风格 = 长相；论证模式 = 论证**。任何模式可配任何视觉风格（一个 `pyramid` 结论先行的 deck，可以看起来 `swiss-minimal` 也可以 `dark-tech`）。「keynote 风」是**模式**（showcase 节奏），不是视觉风格。

### 2. 风格不带 HEX（行为与身份解耦）——三层 token 形态（2026-08-29 落地）

颜色系统分三层，层间单向覆盖（高层覆盖低层）：

| 层 | 载体 | 职责 |
|---|---|---|
| ① 护栏层 | `通用设计规范.md` 配色铁律（三制式 / 60-30-10 / 对比度 / 双重编码） | 怎么用色的纪律，任何风格必过 |
| ② 风格角色默认值 | brief `color_palette`（散文用法说明 + 内嵌 HEX 锚点） | 该风格的缺省建议色；**HEX 是可覆盖的默认，不是风格固有属性** |
| ③ deck 锚点覆盖 | `style render --color role=#RRGGBB`（deck colors / 品牌链路） | 锁定实际色值身份；非法 role / 非 HEX / role 缺失一律 `style_color_override_invalid` 清晰失败 |

覆盖优先级（跨方案声明）：显式 `--color`（本次调用）> 用户品牌（`--brand`，规划中，多行业线）> 风格角色默认值。
11 套顶层内置风格的 palette 已定型为四角色均含 HEX 锚点；126 份子目录参考风格渐进迁移
（lint 白名单登记，见 `scripts/style-lint-baseline.txt` 收敛纪律）。

> 反例（整改前形态）：旧库 139 个风格 brief 把「风格（行为）」和「调色板（身份）」耦合在
> 写死的 HEX 里。三层化后：风格描述「怎么用色」，色值是可覆盖的默认建议。

### 2a. 明暗配对机制（light/dark）

同一风格的 light / dark 变体**不另立风格**，由 palette 角色推导：背景/正文互换（primary ↔
neutral 的纸墨对调）、强调色保持角色不变但按对比度复检（正文 ≥4.5:1、大字与非文本 ≥3:1）；
dark 变体的网格线/hairline 取亮色低饱和。deck 级明暗切换通过 `--color` 逐角色覆盖实现
（dark-deck 预设变体归多行业线，见风格系统优化计划 Deferred）。

### 3. 视觉风格 ↔ 图片渲染 配对

图片式 PPT 的关键：每个视觉风格配一个「图片渲染」风格，让 **AI 生成的图与 SVG 布局共享同一美学**。

| 视觉风格 | 配对图片渲染 |
|---|---|
| swiss-minimal | minimalist-swiss |
| glassmorphism | glassmorphism |
| blueprint | blueprint |
| editorial | editorial |
| memphis | flat |
| zine | screen-print |
| ink-wash | ink-notes / watercolor |
| sketch-notes | sketch-notes |

## 二、六轴清单

| 轴 | 数量 | 现有/待补 | 位置 |
|---|---|---|---|
| 视觉风格 visual-style | 37+11 | 现有（JSON brief，palette 已三层化） | `01_通用母版/` + 顶层 |
| 论证模式 mode | 5 | 已落地（markdown 分节） | `06_论证模式/` |
| 信息图类型 image-type | 11 | 已落地（markdown 三节） | `07_信息图类型/` |
| 图片渲染 rendering | 20 | 已落地（markdown，paste-ready） | `08_图片渲染/` |
| 结构布局 layout | 7 | 已落地（画布 + 页面版式清单） | `09_结构布局/` |
| 品牌身份 brand | 20 | 已落地（企业 VI） | `10_品牌身份/` |
| 图表语法 chart-syntax | 15 | 已落地（mermaid，文本可执行） | `11_图表语法/` |

> **轴结构现状（2026-08-29 更新）**：论证模式/信息图类型/图片渲染/结构布局/品牌身份/图表语法六轴文档均已落地为 markdown 分节文件；**「不带 HEX 解耦」已以三层 token 形态落地**（见上 §2：brief 保持 JSON 格式、palette 为可覆盖的角色默认值，deck 级 `--color` 覆盖通道已实现，顶层内置 11 套已定型 HEX 锚点）；视觉风格的「六节 markdown 格式」仍为 JSON brief 形态，作为长期演进方向保留。下列「待补轴清单」保留作为来源记录。

### 待补轴清单（从 ppt-master 借鉴，后续按需落地）

- **结构布局 layout（7）**：presentation_core(16:9×20 版式)、presentation_core_43(4:3×16)、report_core(16:9×13)、editorial_bleed(16:9×10)、story_vertical(9:16×9)、xiaohongshu_post(3:4×10)、moments_square(1:1×8)
- **品牌身份 brand（20）**：mckinsey、bcg、bain、deloitte、pwc、accenture、google、microsoft、aws、nvidia、ibm、anthropic、huawei、alibaba、tencent、xiaomi、中国电信、中国电建、中汽研、jpmorgan
- **图片渲染 rendering（20）**：vector-illustration、flat、minimalist-swiss、glassmorphism、3d-isometric、digital-dashboard、corporate-photo、blueprint、editorial、sketch-notes、ink-notes、chalkboard、paper-cut、screen-print、pixel-art、vintage-poster、watercolor、nature、warm-scene、fantasy-animation
- **信息图类型 image-type（11）**：infographic、flowchart、framework、matrix、cycle、funnel、pyramid、comparison、timeline、map、scene

## 三、文档格式规范

新轴（论证模式 / 待补轴）采用 **markdown 分节**，非 JSON brief：

**视觉风格文件 · 六节**（`shape & decoration` / `typography` / `using colors`（不带 HEX）/ `texture & elevation` / `paired rendering` / `illustration propensity`）

**论证模式文件 · 四节**（`narrative skeleton` / `page-structure tendencies` / `speaker-notes register` / `page skeleton example`）

**信息图类型文件 · 三节**（`composition skeleton` / `text-policy variants` / `fewshot prompt snippet`）

> 旧库 139 个 JSON brief 保留为「整合版」可直接用；新轴逐步用分节格式落地，两者并存，后续按需迁移。

## 四、现有库 → 六轴映射

| 现有目录 | 映射到 |
|---|---|
| 顶层 11 内置 + `01_通用母版/`（37） | 视觉风格轴 |
| `02_行业内容域/`（46） | 品牌/行业身份轴（行业惯例，待与品牌 VI 合并为「身份轴」） |
| `03_场景用途结构/`（40） | 论证模式轴的「用途场景」细化 |
| `04_来源_guizang/` `05_来源_awesome/` | 来源参考 |
| `06_论证模式/`（5，本轮新增） | 论证模式轴 |
