# 终审 B · 对抗审查（结论：否掉 v1.5）

- 对象：PRD v1.5（345 行）· 立场：尽力否掉，找前三轮因框架而漏掉的东西
- 日期：2026-09-11 · 独立审查（含可复现实验）

## 不可证伪与空转清单（12 项）

R-70a 门「成本不高于基线」——「基线」无定义无采集点（R-77 最小子集验收三条全【单测】，无一条要求产出基线数值）；盲评「融合度单项线」是空白数值、「非劣 −0.5」无判定式、「组」的样本量未定义；TF「预登记口径」无格式/路径/冻结时点；「样张重确认不上升、返工轮次下降」无基线期/窗长/N/阈值且被贴【run-留痕】标签（证据强度错配）；「合成时长上限实测披露」用披露替换上限；R-71 效果验收同 R-70a 病且依赖未声明；**「缺失指标如实 not_recorded」是万能通过出口**（全缺仍满足）；「一致率 ≥70% 或逐条归因」的「或」给出免费通行证；「注册表完整性」无判据；**「沉没成本清单」在文档与附录中根本不存在**（全文仅出现在这一句）；三处【人工-协议】都要求"协议留档"而协议本体只在空白标题 A1；「三路径语义成文」不可判。
→ 结论：除 R-83、R-80 外，全批非目标与门线**没有检查者**。

## 依赖链致命单点（5 项）

- **A（致命）**：R-77 最小子集 → 全批，而其验收不允许行为级证据且保留全缺出口 ⇒ 测量底座可空转完成，随后所有门无法执行。**三代评审都没问「波 1 怎样才算失败」**。
- **B（致命）**：回放集——被 R-70 验收 4、盲评对照分层、代理指标、R-71 效果验收共同依赖，却**只出现在 R-70 自己的验收句里**（R-77 验收零断言、§8 工程量无行、交付无门）；校准集 `defect-controls`（§1.5 已承认物理未落盘）在 §8 R-70 工程量中根本没列。
- **C（未声明）**：母版 schema 五处写入（R-70b/R-71/R-74/R-75/R-82）无属主无冻结顺序（而 page_type_regime 与呈现/披露都有属主）。
- **D（未声明）**：真实 run 吞吐量——效力反算、基线门、R-73 校准集、R-83 重启判据全部隐含"有足够历史 run"，全文档无一处 run 量假设，而这是唯一无法用工程量换取的资源。
- **E（自相矛盾）**：§1.5「每域单锁＋全局 revision CAS，每波仅一人成功」vs R-81 净影响「用满并行度即可削减等待」——同域并行不成立，关闭后无条目负责验证该主张。

## 悬置决策（12 项，3 项必须开工前定）

**必须开工前定**：①`text_fallback` 补登路线（决定 TF 分子是否存在，影响效力反算基数）②偏离率基线（top2 / suggest_layout，同时是 R-71 缺省绑定消费口径）③盲评 verdict schema 版本冻结（它是 R-70a 门的量具，却不在先决清单里）。另：`defect-controls` 构造与归属、回放集所有权、「降级或关闭」之动作定义（R-70a 已是降级形态，再降不成立）、`page_type` 双义收敛。
**可延后**：R-74 mid-run 切换、R-82 时长来源（但须在 R-70b 母版冻结前回填）、R-80 披露形态、「每评审周期」的周期数值。

## 体量与放弃判据

波 1 实际＝3 个前置 spike＋R-77 最小子集（13 项注册表＋§7 共享聚合抽取）＋R-71（编排级浏览器/字体复用＝**新组件**）＋R-70a/b/c（42 版式 media 槽、3 版式×2 主题资产、透明文字层、双 provenance、离线字体主题化、盲评图片通道＋评分 schema＋匿名化先例＋校准集构造）＋R-76 一期＋R-84＋8 项合同修订面分 commit＋六处外溢触点；§8 的 L 估算**漏项**（校准集、overlay 主题化、匿名化先例、判官 spike 均未计入）。文档无工时/WIP 上限/里程碑假设 → 「只做 2~3 条」的收窄只发生在纸面。
**放弃判据只有两条**（基线否决门、R-73 scope 门），**R-70a/b/c 三阶段门无一是停止门**；v1.3 引入的效力反算本为"不可达则退出"，v1.5 换成了"不可达则换尺"。**R-70 的放弃判据在三轮中被逐轮删除而非补全。**

## 治理可用性（实测事实）

1. `page-type-regime-v1.json`（12:25）、`page_intent.py`（12:26）、`tests/test_page_intent_routing.py`（12:34）、`docs/plans/2026-09-11-002-*.md`（12:34）**全部处于未提交工作区**，`CHANGELOG [Unreleased]` 已登记；PRD v1.5 写于 12:39，**零提及**。
2. **命名直接冲撞**：PRD 的 `page_type_regime`（页型枚举×lane）vs CLI `--page-type`（chart/text-heavy/image）——同一术语两个真值源，且已落地物占用了该名字（其键为 `page_types`，值为 13 类语义形态，`allowed_lanes` 只有 image/render:html）。
3. **§8 状态已被证伪**：R-74「状态＝待页型 regime」在写出时即假（`layout-dispatch.md` 已把 regime 作为第一打分输入）。
4. 该真值源**不在任何 lint 覆盖内**（`lint_style_briefs.py` 扫描面不含 `governance/rules/`）；**回归不可被仓库运行器收集**——`unittest discover` 在 tests/ 发现 1845 项，`test_page_intent_routing.py` 贡献 **0** 项（该文件是本目录唯一用裸 `def test_` 且无 `__main__` 的），venv 内未安装 pytest。plan 文档「45 项通过」因此只证明既有套件通过。
5. §8 表「状态」列取值**无一是机器可判的**；L6/L12 引用不存在的终审纪要；自称「自包含」不成立（TF-1/TF-2、3a、U8、WS6、M0、u19、五类指纹类名均无术语表，定义只存在于 15 份报告中）。

## Findings（17 项）

G2-1 blocker 并发落地冲撞且零处理（含改名收敛与两条治理条款）；G2-2 blocker R-77 最小子集可零可用数字通关（含第 4 条验收文字与 blocked 语义）；G2-3 blocker 回放集与校准集无归属无验收工程量漏项；G2-4 major 效力降级无判定语义（含可判形式改写）；G2-5 major 沉没成本清单不存在＋「禁止临场放宽」无检查者（含 §9.1 新节与冷却期）；G2-6 major 三项决策必须开工前定（含冻结决策表）；G2-7 major 母版 schema 无属主；G2-8 major R-81 净影响与源码矛盾；G2-9 major `suggest_layout` 语义加分被 1.0 clamp 饱和（实测：首选 compare 与回退 P21 `score` 同为 1.0，顺序由破并列决定）；G2-10 major 盲评缺三个可判定要素；G2-11 major 测试入口标签与证据强度错配（建议新增两档）；G2-12 major 并发真值源无 lint 无回归收集；G2-13 minor §2.5「每条至少 1 正 1 负」被 R-78/R-79 违反；G2-14 minor 自包含不成立（无术语表、A1/A2/A6 空标题）；G2-15 minor 预览路径三分指纹语义相反无收敛规则；G2-16 minor §8 表不可用于跟踪且已被证伪一次；G2-17 minor 体量无产能预算、R-70 无阶段止损。

## 终审结论

**否掉（reject v1.5，要求 v1.6 重提）**。理由三条结构性事实：①文档对现实的描述已失真（并发落地未登记、状态被证伪、术语同名异义）——终审对象与被治理对象不在同一时空；②全批测量底座可以"什么都不测出"而宣告完成；③唯一效力出口被三轮逐次删除、最大赌注条目没有止损。
**最短重提路径**：治理两条条款＋基线增补＋改名收敛；R-77 增第 4/5 条验收并删除 not_recorded 通过地位；R-70 先决扩四项＋判定式可判化＋三阶段止损；沉没成本清单与三项冻结决策入文；母版 schema 属主与盲评三数值；标签两档；R-74 去饱和断言——全部文档级，不需新代码。若选择只做 R-77 最小子集＋R-71（R-70 推入波 2），则 R-70 相关条件可延后，其余仍为放行前提。
