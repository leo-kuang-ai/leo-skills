# 设计体系（元结构）

> 借鉴 `ppt-master`（hugohe3/ppt-master）的四轴正交模型，重构 leo-ppt 风格库的认知结构。核心结论：**风格不是「一个完整配方」，而是「几个正交轴的组合」**。

## 一、核心原则

### 1. 轴正交，任意组合

一个 deck 的最终视觉，由**四个独立轴**分别锁定，再任意组合：

| 轴 | 回答 | 例子 |
|---|---|---|
| **视觉风格** visual-style | 长什么样（形状/留白/纹理/排版） | swiss-minimal、glassmorphism、editorial、ink-wash |
| **论证模式** mode | 怎么论证（叙事骨架/页面节奏） | pyramid、narrative、instructional、showcase、briefing |
| **结构布局** layout | 什么画布 + 什么版式骨架 | 16:9 / 4:3 / 9:16 / 3:4 / 1:1 |
| **品牌身份** brand | 谁的身份（企业 VI / 行业惯例） | mckinsey、bcg、google、huawei |

> 关键区分：**视觉风格 = 长相；论证模式 = 论证**。任何模式可配任何视觉风格（一个 `pyramid` 结论先行的 deck，可以看起来 `swiss-minimal` 也可以 `dark-tech`）。「keynote 风」是**模式**（showcase 节奏），不是视觉风格。

### 2. 风格不带 HEX（行为与身份解耦）——三层 token 形态（2026-08-29 落地）

颜色系统分三层，层间单向覆盖（高层覆盖低层）：

| 层 | 载体 | 职责 |
|---|---|---|
| ① 护栏层 | `通用设计规范.md` 配色铁律（三制式 / 60-30-10 / 对比度 / 双重编码） | 怎么用色的纪律，任何风格必过 |
| ② 风格角色默认值 | brief `color_palette`（散文用法说明 + 内嵌 HEX 锚点） | 该风格的缺省建议色；**HEX 是可覆盖的默认，不是风格固有属性** |
| ③ deck 锚点覆盖 | `style render --color role=#RRGGBB`（deck colors / 品牌链路） | 锁定实际色值身份；非法 role / 非 HEX / role 缺失一律 `style_color_override_invalid` 清晰失败 |

覆盖优先级（跨方案声明）：显式 `--color`（本次调用）> 用户品牌（`--brand`，已实现，合同见 [`style-library.md`](../../../../references/style-library.md)）> 风格角色默认值。
11 套顶层内置风格的 palette 已定型为四角色均含 HEX 锚点；126 份子目录参考风格渐进迁移
（lint 白名单登记，见 `scripts/style-lint-baseline.txt` 收敛纪律）。

> 反例（整改前形态）：旧库 139 个风格 brief 把「风格（行为）」和「调色板（身份）」耦合在
> 写死的 HEX 里。三层化后：风格描述「怎么用色」，色值是可覆盖的默认建议。

### 2a. 明暗配对机制（light/dark）

同一风格的 light / dark 变体**不另立风格**，由 palette 角色推导：背景/正文互换（primary ↔
neutral 的纸墨对调）、强调色保持角色不变但按对比度复检（正文 ≥4.5:1、大字与非文本 ≥3:1）；
dark 变体的网格线/hairline 取亮色低饱和。deck 级明暗切换通过 `--color` 逐角色覆盖实现
（dark-deck 预设变体归多行业线，见风格系统优化计划 Deferred）。

### 3. 视觉风格 ↔ 图片渲染 配对

图片式 PPT 的关键：每个视觉风格配一个「图片渲染」风格，让 **AI 生成的图与 SVG 布局共享同一美学**。

| 视觉风格 | 配对图片渲染 |
|---|---|
| swiss-minimal | minimalist-swiss |
| glassmorphism | glassmorphism |
| blueprint | blueprint |
| editorial | editorial |
| memphis | flat |
| zine | screen-print |
| ink-wash | ink-notes / watercolor |
| sketch-notes | sketch-notes |

### 3a. 轴间组合护栏（Avoid With）

> 来源：baoyu-skills dimensions 各轴 Combination Notes（JimLiu 原创部分，MIT 直引词表、
  表述 leo 化改写；快照 6b7a2e4 2026-07-03）。护栏只做预筛与**一次劝阻**（对齐
  style-recommendation §四「冲突劝阻一次，然后尊重」合同），不做硬失败。目标态对齐
  [目标架构](../../../../../docs/leo-ppt-generator/architecture/style-library-target-architecture.md)
  §4.5 bindings 兼容字段（`rendering_id` + `compatible_mode_ids`），本散文表为 v1
  过渡层，避免成为第三份手维护映射。

| 组合 | 忌配 | 理由 |
|---|---|---|
| pixel 质感视觉风格 | editorial 高对比衬线标题 | 像素栅格与高对比衬线笔形互斥（库内无反例） |
| paper 做旧质感 | minimal 密度（geometric 无衬线仅作提示项，不作硬忌配） | 冲突核是做旧媒介感 × 极简留白；复古海报存在合法 geometric+paper 组合 |
| 手绘系图片渲染 | 数据仪表盘视觉风格 | 商务/审计/汇报数据页中手绘介质削弱数据可信度；教学/科普语境除外（数学可视化教学风先例） |
| poly-pop 制式 × `--brand` 单色覆盖 | —— | 提示一次制式差异（「强调=最大对比对」语义将失效），提示后按三层 token 覆盖优先序执行，不设确认门 |
| 台账档密度（≤6 条档） | 陈述/金句版式（P3/P10/P34/P36） | 容量语义相反，预检即拦 |
| grid 工程纸质感 | organic 有机插画渲染 | 工程秩序 vs 手绘随意，混质 |

### 3b. 质感档标注维度（七档）

> 来源：baoyu-skills dimensions texture 五档扩写（MIT 口径同上；glass/glow 两档为本库
  补齐）。纪律对齐六分组先例全四条：只作标注与预筛，不新建条目、不占风格配额、
  不改 `variant_of` 主维度语义、无专用 schema 键。**本维度非穷举——档外质感如实
  留空，仅作预筛不硬归类**。档位写进 brief 散文 `canvas.background`（与六分组的
  明度档写 `visual_direction` 分工：明度归视觉方向、质感归画布底面）。判据为
  底材质感与画面语法合参（部分风格的网格/像素在元素层而非底面）。

| 档 | 判据（底面与语法合参） | 典型风格系 | 忌配（接 §3a） |
|---|---|---|---|
| `clean` | 纯色无纹理底 | 极简/商务/终端 | — |
| `grid` | 工程纸网格线（底面或骨架语法） | 蓝图/实验室标签 | organic 渲染 |
| `organic` | 纸纹画布、不完美边缘 | 手账/水彩/温暖手工 | pixel |
| `pixel` | 块状像素语法（画面层面） | 像素复古/复古电视 | editorial 衬线 |
| `paper` | 做旧纸、印刷瑕疵、sepia | 博物/vintage/旧学院 | minimal 密度 |
| `glass` | 磨砂/透明层叠底面 | 玻璃拟态 | pixel |
| `glow` | 渐变光效/发光底面 | 荧光科技/暗夜氛围 | paper |

## 二、六轴清单

| 轴 | 数量 | 现有/待补 | 位置 |
|---|---|---|---|
| 视觉风格 visual-style | 320（registry 当前快照） | 已落地（v2 JSON brief） | `template-library/canonical/styles/` |
| 论证模式 mode | 19 | 已落地（manifest + body） | `template-library/canonical/axes/argument/` |
| 信息图类型 image-type | 11 | 已落地（manifest + body） | `template-library/canonical/axes/infographic/` |
| 图片渲染 rendering | 43 | 已落地（manifest + body） | `template-library/canonical/axes/rendering/` |
| 结构布局 layout | 42 个 profile | 已落地（profile + notes） | `template-library/canonical/layouts/` |
| 品牌身份 brand | 36 | 已落地（企业 VI） | `template-library/canonical/brands/` |
| 图表语法 chart-syntax | 18 | 已落地（manifest + body） | `template-library/canonical/axes/chart/` |

> **轴结构现状（2026-09-09）**：活动资产以 `template-library/canonical/` 为唯一真值，轴文档采用 manifest + body，版式采用 `layout-profile-v1`，风格采用 `style-brief-v2`。`catalog/current.json` 指向确定性 registry；其中统计会随发布代更新，不能手工维护。颜色仍按护栏、风格默认值、deck/brand 覆盖三层处理；视觉验证状态由 evidence 包派生，不由 brief 或 registry 冒充。下列「待补轴清单」仅保留为来源记录。

### 待补轴清单（从 ppt-master 借鉴，后续按需落地）

- **结构布局 layout（7）**：presentation_core(16:9×20 版式)、presentation_core_43(4:3×16)、report_core(16:9×13)、editorial_bleed(16:9×10)、story_vertical(9:16×9)、xiaohongshu_post(3:4×10)、moments_square(1:1×8)
- **品牌身份 brand（20）**：mckinsey、bcg、bain、deloitte、pwc、accenture、google、microsoft、aws、nvidia、ibm、anthropic、huawei、alibaba、tencent、xiaomi、中国电信、中国电建、中汽研、jpmorgan
- **图片渲染 rendering（20）**：vector-illustration、flat、minimalist-swiss、glassmorphism、3d-isometric、digital-dashboard、corporate-photo、blueprint、editorial、sketch-notes、ink-notes、chalkboard、paper-cut、screen-print、pixel-art、vintage-poster、watercolor、nature、warm-scene、fantasy-animation
- **信息图类型 image-type（11）**：infographic、flowchart、framework、matrix、cycle、funnel、pyramid、comparison、timeline、map、scene

## 三、文档格式规范

新轴（论证模式 / 待补轴）采用 **markdown 分节**，非 JSON brief：

**视觉风格文件 · 六节**（`shape & decoration` / `typography` / `using colors`（不带 HEX）/ `texture & elevation` / `paired rendering` / `illustration propensity`）

**论证模式文件 · 四节**（`narrative skeleton` / `page-structure tendencies` / `speaker-notes register` / `page skeleton example`）

**信息图类型文件 · 三节**（`composition skeleton` / `text-policy variants` / `fewshot prompt snippet`）

> 旧库的 311 份来源 brief 已迁移为 canonical 的 v2 实体；当前 registry 的 style 数量以
> `catalog/current.json` 指向代为准。轴文档继续使用 manifest + body，两者的内容格式不同但
> 身份、来源和生命周期都由 canonical/governance 契约管理。

## 四、现有库 → 六轴映射

| 现有目录 | 映射到 |
|---|---|
| `canonical/styles/`（当前 registry style 实体） | 视觉风格轴 |
| `canonical/styles/*/brief.json` 的 taxonomy + `governance/rules/domains/` | 品牌/行业身份轴 |
| `canonical/axes/structure/` | 论证模式轴的用途场景细化 |
| `reference/sources/`、`reference/candidates/`、`reference/pools/` | 来源参考（不具备执行资格） |
| `canonical/axes/argument/` | 论证模式轴 |
